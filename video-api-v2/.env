# DOMAIN_NAME and SUBDOMAIN together determine where sub will be reachable from
# The top level domain to serve from
DOMAIN_NAME=example.com

# The subdomain to serve from
SUBDOMAIN=sub

# The above example serve sub at: https://sub.example.com

# Optional timezone to set which gets used by Cron and other scheduling nodes
# New York is the default value if not set
GENERIC_TIMEZONE=Europe/Berlin

# The email address to use for the TLS/SSL certificate creation
SSL_EMAIL=user@example.com

# Traefik Auth (создать хеш пароля)
TRAEFIK_USER=admin
TRAEFIK_PASSWORD_HASH=$$2y$$12$$xxxxxxxxxxxxxxxx

# n8n Encryption
N8N_ENCRYPTION_KEY=your-secure-random-key-32-chars